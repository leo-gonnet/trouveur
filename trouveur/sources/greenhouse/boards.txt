# Greenhouse board slugs, one per line. `#` starts a comment.
#
# This is the ONLY list of boards that exists: Greenhouse publishes no index of its tenants, so a
# company is crawled if and only if its slug appears here. The slug is the last path segment of a
# company's careers URL, e.g. job-boards.greenhouse.io/<slug>.
#
# Verify a slug before adding it — an unknown one returns HTTP 404 and shows up as a failing scope
# on the dashboard every day until someone removes it:
#
#     curl -s -o /dev/null -w '%{http_code}\n' \
#       https://boards-api.greenhouse.io/v1/boards/<slug>/jobs
#
# All entries below returned 200 on 2026-09-08.

adyen
celonis
contentful
doctolib
getyourguide
gitlab
hellofresh
monzo
n26
sumup
wise
